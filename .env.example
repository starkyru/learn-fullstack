# Postgres (docker-compose default)
DATABASE_URL="postgresql://dev:dev@localhost:5432/learn_fullstack?schema=public"

# Auth
AUTH_SECRET="change-me-in-dev-only-32-chars-min"
JWT_SECRET="change-me-in-dev-only-32-chars-min"

# OAuth (module 21 / kanban capstone) — fill when needed
GITHUB_ID=""
GITHUB_SECRET=""

# Optional Redis (chat sessions / pub-sub)
REDIS_URL="redis://localhost:6379"

NODE_ENV="development"
