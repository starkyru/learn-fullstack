/**
 * YOUR TURN — return the focusable elements inside `container`, in DOM order, for a focus
 * trap. Include: a[href], button, input, select, textarea, and [tabindex] — but EXCLUDE
 * anything disabled or with tabindex="-1". Use `container.querySelectorAll` + filter.
 */
export function getFocusableElements(_container: HTMLElement): HTMLElement[] {
  throw new Error("TODO: query + filter the focusable descendants in DOM order");
}
