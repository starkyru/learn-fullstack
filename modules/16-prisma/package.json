{
  "name": "@learn-fullstack/mod-16-prisma",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "prisma generate",
    "db:generate": "prisma generate",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@prisma/adapter-better-sqlite3": "catalog:",
    "@prisma/client": "catalog:",
    "@types/node": "catalog:",
    "prisma": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
